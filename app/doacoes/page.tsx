'use client';
import { QRCodeCanvas } from 'qrcode.react';
import { pixKey } from '@/lib/data';
export default function DoacoesPage(){ async function copy(){ await navigator.clipboard.writeText(pixKey); alert('Chave Pix copiada!'); } return <section className="container-page py-16"><span className="badge bg-coral/20">Doações</span><h1 className="section-title mt-5">Ajude os Pequenos Adoradores</h1><p className="section-subtitle">Sua contribuição apoia materiais, eventos, EBF, atividades, estrutura, cantina e cuidado com nossas crianças.</p><div className="mt-10 grid gap-8 lg:grid-cols-2"><div className="card p-8"><h2 className="text-3xl font-black">Chave Pix</h2><p className="mt-4 rounded-2xl bg-sol/30 p-4 text-xl font-black break-all">{pixKey}</p><button onClick={copy} className="btn-primary mt-6">Copiar chave Pix</button></div><div className="card grid place-items-center p-8"><QRCodeCanvas value={pixKey} size={260} includeMargin/><p className="mt-4 text-center font-bold text-slate-700">Aponte a câmera para doar</p></div></div></section>}
